# https://docs.amvera.ru/applications/environments/nodejs-server.html
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build

run:
  scriptName: index.js
  containerPort: 80
