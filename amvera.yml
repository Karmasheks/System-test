meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: node scripts/prepare-amvera.mjs && npm install && npm run build

run:
  scriptName: index.js
  containerPort: 80
