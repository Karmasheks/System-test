meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: sh -c 'cd /source && rm -rf dist && npm run build && test -f dist/index.js'
  artifacts:
    "dist/**": /
    "node_modules/**": /
    "index.js": /
    "package.json": /
    "package-lock.json": /
    "uploads/**": /

run:
  scriptName: index.js
  containerPort: 80
