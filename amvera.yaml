# https://docs.amvera.ru/applications/environments/nodejs-server.html
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build

run:
  # Не задавайте в панели command с --import tsx — только npm run start
  command: npm run start
  containerPort: 80
