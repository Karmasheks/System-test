meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: AMVERA_CLEAN=1 node scripts/prepare-amvera.mjs && npm install && npm run build

run:
  scriptName: index.js
  containerPort: 80
