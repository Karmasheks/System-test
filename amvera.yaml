meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  # Чистая сборка: не использовать dist/node_modules из артефакта Amvera
  additionalCommands: rm -rf dist && npm run build

run:
  scriptName: index.js
  containerPort: 80
